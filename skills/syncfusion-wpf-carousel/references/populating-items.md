# Populating Items (split)

This file was split into two smaller reference pages to stay within size limits:

- `populating-items-basics.md`: direct `CarouselItem`, collection binding, and `ItemTemplate` usage.
- `populating-items-advanced.md`: container styles, style selectors, virtualization, selection APIs, and flow direction.

See the two files above for the complete content previously contained here.
